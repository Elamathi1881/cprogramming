TREE:-


#include<stdio.h>
#include<stdlib.h>
struct node{
    int data;
    struct node *left;
    struct node *right;
};
struct node* createnode(int value){
    struct node*newnode=(struct node*)malloc(sizeof(struct node));
    newnode->data=value;
    newnode->left=NULL;
    newnode->right=NULL;
    return newnode;
    
}
int main(){
    struct node*root=createnode(10);
    root->left=createnode(20);
    root->right=createnode(30);
    root->left->left=createnode(40);
    root->left->right=createnode(50);
    root->right->left=createnode(60);
    root->right->right=createnode(70);
    printf("root value=%d\n",root->data);
    printf("root->left->right=%d\n",root->left->right->data);
    return 0;
    
}
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

problem 1:-


 

#include<stdio.h>
#include<stdlib.h>
#include<string.h>
struct node{
    char data[20];
    struct node *left;
    struct node *right;
};
struct node* createnode(char value[]){
    struct node*newnode=(struct node*)malloc(sizeof(struct node));
    strcpy(newnode->data,value);
    newnode->left=NULL;
    newnode->right=NULL;
    return newnode;
    
}
int main(){
    struct node*CEO=createnode("CEO");
    CEO->left=createnode("Manager1");
    CEO->right=createnode("Manager2");
    CEO->left->left=createnode("TeamLead1");
    CEO->right->right=createnode("TeamLead2");
    printf("CEO=%s\n",CEO->data);
    printf("CEO->left->right=%s\n",CEO->right->right->data);
    return 0;
    
}
-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
problem-2



#include<stdio.h>
#include<stdlib.h>
#include<string.h>
struct node{
    char data[20];
    struct node *left;
    struct node *right;
};
struct node* createnode(char value[]){
    struct node*newnode=(struct node*)malloc(sizeof(struct node));
    strcpy(newnode->data,value);
    newnode->left=NULL;
    newnode->right=NULL;
    return newnode;
    
}
int main(){
    struct node*grandmother=createnode("john");
    grandmother->left=createnode("parent1");
    grandmother->right=createnode("parant2");
    grandmother->left->left=createnode("david");
    grandmother->left->right=createnode("alex");
    printf("grandmother=%s\n",grandmother->data);
    printf("First child:%s\n", grandmother->left->left->data);
    printf("Second child:%s", grandmother->left->right->data);
    return 0;
    
}
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
problems:- 3



#include<stdio.h>
#include<stdlib.h>
#include<string.h>
struct node{
    char data[20];
    struct node *left;
    struct node *right;
};
struct node* createnode(char value[]){
    struct node*newnode=(struct node*)malloc(sizeof(struct node));
    strcpy(newnode->data,value);
    newnode->left=NULL;
    newnode->right=NULL;
    return newnode;
    
}
int main(){
    struct node*products=createnode("products");
    products->left=createnode("electronics");
    products->right=createnode("clothing");
    products->left->left=createnode("mobiles");
    products->left->right=createnode("laptops");
    printf("main category:%s\n",products->data);
    printf("electronics sub-category:%s\n", products->left->left->data);
    printf("electronics sub-category:%s", products->left->right->data);
    return 0;
    
}
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

preorder:-

#include<stdio.h>
#include<stdlib.h>
struct node{
    int data;
    struct node *left;
    struct node *right;
};
struct node* createnode(int value){
    struct node*newnode=(struct node*)malloc(sizeof(struct node));
    newnode->data=value;
    newnode->left=NULL;
    newnode->right=NULL;
    return newnode;
    
}
void preorder(struct node* root){
    if(root==NULL)
    return;
    printf("%d",root->data);
     preorder(root->left);
      preorder(root->right);
    
}
int main(){
    struct node*root=createnode(10);
    root->left=createnode(20);
    root->right=createnode(30);
    root->left->left=createnode(40);
    root->left->right=createnode(50);
    root->right->left=createnode(60);
    root->right->right=createnode(70);
     preorder(root);
    return 0;
    
}
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

